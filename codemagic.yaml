workflows:
  ios-capacitor:
    name: MarktFuchs iOS
    instance_type: mac_mini_m2
    max_build_duration: 60

    environment:
      node: 22
      xcode: latest
      cocoapods: default

    scripts:

      - name: Install dependencies
        script: |
          cd frontend
          npm install --legacy-peer-deps

      - name: Install Capacitor
        script: |
          cd frontend
          npm install @capacitor/core@8.5.2 @capacitor/cli@8.5.2 @capacitor/ios@8.5.2 --save --legacy-peer-deps

      - name: Create Capacitor config
        script: |
          cd frontend

          cat > capacitor.config.ts <<'EOF'
          import type { CapacitorConfig } from '@capacitor/cli';

          const config: CapacitorConfig = {
            appId: 'com.marktfuchs.app',
            appName: 'MarktFuchs',
            webDir: 'dist'
          };

          export default config;
          EOF

      - name: Build web app
        script: |
          cd frontend
          npm run build

      - name: Add iOS platform
        script: |
          cd frontend
          rm -rf ios
          npx cap add ios

      - name: Sync Capacitor
        script: |
          cd frontend
          npx cap sync ios

      - name: Check iOS project
        script: |
          cd frontend
          echo "=== Generated iOS project ==="
          find ios -maxdepth 5 -type f | sort
          echo "=== Podfiles ==="
          find ios -name "Podfile" -print
          echo "=== Workspaces ==="
          find ios -name "*.xcworkspace" -print

      - name: Build iOS
        script: |
          cd frontend

          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            build

    artifacts:
      - frontend/ios/**/*.app
      - /tmp/xcodebuild_logs/*.log
